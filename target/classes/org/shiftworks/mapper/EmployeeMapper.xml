<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.shiftworks.mapper.EmployeeMapper">

	<resultMap type="org.shiftworks.domain.EmployeeVO" id="empMap">
	
		<id property="dept_id" column="dept_id"/>
		<result property="emp_id" column="emp_id"/>
		<result property="dept_id" column="dept_id"/>
		<result property="name" column="name"/>
		<result property="position" column="position"/>
		<result property="mobile" column="mobile"/>
		<result property="internal" column="internal"/>
		<result property="email" column="email"/>
		<collection property="userDTO" resultMap="userMap"/>
	
	</resultMap>
	
		<resultMap type="org.shiftworks.domain.UserDTO" id="userMap">
	
		<result property="emp_id" column="emp_id"/>
		<result property="dept_id" column="dept_id"/>
		<result property="authority" column="authority"/>
	
	</resultMap>

	<select id="read" resultType="org.shiftworks.domain.EmployeeVO">
	
		select emp_id, emp.dept_id, authority from employee emp join department dept 
			on emp.dept_id = dept.dept_id
			where emp_id = #{emp_id}
	
	</select>

</mapper>
