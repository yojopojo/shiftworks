<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.shiftworks.mapper.ScheduleMapper">

	<insert id="insertSchedule">
		INSERT INTO schedule
			VALUES(schedule_seq.nextval, #{dept_id}, #{book_id}, #{sch_title}, #{sch_content},
					#{start_date}, #{end_date}, #{group}, #{sch_writer})
	</insert>
	
	<insert id="insertPartipant">
		
		<foreach collection="participant" item="p">
			INSERT INTO sch_participant
				VALUES(#{p.emp_id}, #{p.dept_id}, #{p.sch_id}, #{p.notification})
			
		</foreach>
	
	</insert>
	
	<delete id=""></delete>

</mapper>