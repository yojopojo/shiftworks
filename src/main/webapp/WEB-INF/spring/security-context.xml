<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd
	http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<bean id ="customAccessDenied" class="org.shiftworks.security.CustomAccessDeniedHandler"/>

<!-- <bean id ="customLoginSuccessHandler" class="org.shiftworks.security.CustomLoginSuccessHandler"/> -->
	
	<bean id="bcryptPasswordEncoder"
		class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
	
	<bean id="customUserDetailsService"
		class="org.shiftworks.security.CustomUserDetailsService"/>
	
	<security:http>
	
		<security:intercept-url pattern="/login/main" access="permitAll"/>
		<security:intercept-url pattern="/login/user" access="hasRole('ROLE_USER')"/>
		<security:intercept-url pattern="/login/admin" access="hasRole('ROLE_ADMIN')"/>

		<!-- <security:form-login/> -->
		<security:form-login login-page="/LOG_userLogin"/>
		
		<security:logout logout-url="/LOG_userLogout" 
			invalidate-session="true"
			delete-cookies="JSESSION_ID"/>
	<!-- <security:access-denied-handler error-page="/accessError"/> -->
	<security:access-denied-handler ref="customAccessDenied"/>
	
	</security:http>
	
	<!-- provider -->
	<security:authentication-manager>
		<security:authentication-provider
			user-service-ref="customDetailsService">
			<!-- <security:user-service>
				<security:user name="user1" password="{noop}shiftworks113" authorities="user"/>
				<security:user name="user4" password="{noop}shiftworks113" authorities="admin"/>			
			</security:user-service> -->
			
			<!-- <security:jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="select emp_id, dept_id, name, password
					from employee where emp_id = ? "/> -->
			
			<security:password-encoder ref="bcryptPasswordEncoder"/>
			
		</security:authentication-provider>
		
	
	</security:authentication-manager>
	

</beans>
